---
deployment:
  tasks:
    - export DEPLOYPATH=/home/natabicq/public_html/
    - /bin/cp -R files $DEPLOYPATH
    - /bin/cp -R images $DEPLOYPATH
    - /bin/cp -R my_pages $DEPLOYPATH
    - /bin/cp -R stylesheets $DEPLOYPATH
    - /bin/cp index.html $DEPLOYPATH
